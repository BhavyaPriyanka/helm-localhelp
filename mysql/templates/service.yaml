apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: localhelp
spec:
  selector:
    app: mysql
    tier: database
    project: localhelp
  ports:
  - protocol: TCP
    port: 3306 #service-port
    targetPort: 3306 #target-port